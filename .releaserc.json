{
  "extends": "@saithodev/semantic-release-sharedconf-npm",
  "plugins": [
    [
      "@semantic-release/exec",
      {"prepareCmd": "pnpm run build && git add dist -f"}
    ],
    [
      "@semantic-release/git",
      {"assets": ["dist"]}
    ]
  ],
  "branches": [
    "master"
  ]
}